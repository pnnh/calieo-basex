import {CBInputCommon, CBInputCommonProps} from "@/components/common/input";

export class CBInputServer extends CBInputCommon {
    static new(props: CBInputCommonProps) {
        return CBInputCommon.new(props)
    }
}